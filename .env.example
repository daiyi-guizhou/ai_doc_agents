# 多 Agent 框架 · LLM 后端配置
# 复制为 .env 后填写；缺 key 或 AGENTS_MOCK=1 时自动走 Mock 模式（离线可跑）。

# 强制 Mock 模式（离线演示 / CI 测试）：填 1 开启；留空则使用真实 LLM
AGENTS_MOCK=

# OpenAI 兼容端点（兼容 OpenAI / Azure / vLLM / Ollama 等）
OPENAI_API_BASE=https://api.openai.com
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
