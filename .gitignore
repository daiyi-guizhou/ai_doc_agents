# Obsidian 个人配置（不进仓库，避免污染文档）
.obsidian/

# 自动生成的巡检报告（由 tools/inspect.py 产出）
docs/.reports/

# 多 Agent 框架运行时产物（每次任务落库，独立于文档治理）
agent_runs/

# Python 字节码缓存（不应进仓库）
__pycache__/
*.pyc

# 本地环境变量（含密钥，绝不进仓库；.env.example 可进）
.env

# WorkBuddy 运行时/记忆数据（本地 Agent 状态，非项目源码）
.workbuddy/
